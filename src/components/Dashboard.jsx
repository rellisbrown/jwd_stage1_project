import React from 'react';
import styled from 'styled-components';

const StyledDashboardContainer = styled.div`
  width: 90%;
  margin: auto;
`;

const Dashboard = () => {
  const test = 1;

  return <StyledDashboardContainer>Test</StyledDashboardContainer>;
};

export default Dashboard;
